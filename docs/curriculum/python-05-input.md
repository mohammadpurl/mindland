# سناریوی درس PY-05 — input و تعامل

شناسه: `python-05-input`

منبع حقیقت:
- `lib/curriculum/scenarios/python-05-input/`
- `lib/pythonErrorMessages.ts` (ValueError / TypeError برای input)
- `PythonCodeRunner` + مودال fallback برای `input()` + چک‌لیست `has-input` / `used-int`

مسیر: `/[lang]/lessons/programming/python-05-input`
