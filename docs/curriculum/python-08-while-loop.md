# سناریوی درس PY-08 — حلقه while

شناسه: `python-08-while-loop`

منبع حقیقت:
- `lib/curriculum/scenarios/python-08-while-loop/`
- `lib/pythonSafeRunner.ts` (محافظ حلقه + ورودی پویا)
- `lib/pythonErrorMessages.ts` (infinite-loop / stale-condition)
- `PythonCodeRunner` + چک‌لیست `has-while` / `while-win` / `has-attempts`

مسیر: `/[lang]/lessons/programming/python-08-while-loop`
